#include <iostream>
#include<array>
#include<iomanip>
using namespace std;

int main()
{
     array<int,10>counts={};
     //(b)Ã‚
     array<int,15>bonus={};
     for(int i=0,i<15,i++)
       {
         bonus[i]=+bonus[i];
       }
     array<double,12>monthlyTemperatures={};
          {
              for(int i=0,i<12,i++)
            cin>>monthlyTemperatures[i];
            )
     array<int,5>bestScores={};
     for(int i=0,i<5,i++)

     {
         cin>>bestScores[i];
     cout<<bestScores[i];
     )
}
